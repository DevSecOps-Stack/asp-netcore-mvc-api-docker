apiVersion: apps/v1
kind: Deployment
metadata:
  name: carma_api
  labels:
    app: carma_api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: carma_api
  template:
    metadata:
      labels:
        app: carma_api
    spec:
      containers:
      - name: carma_api
        image: contosoprodacruwgwaefh.azurecr.io/carma_main-api:148
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: carma_api
spec:
  type: ClusterIP
  selector:
    app: carma_api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80